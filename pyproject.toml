[project]
name = "paincave-thumbnail"
version = "0.1.0"
description = "Local thumbnail generation for Pain Cave tracks"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.28.0",
    "Pillow>=10.0.0",
]
